name: Test Discord Failure Notifier

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Run local Discord Failure Notifier
        uses: ./
        with:
          webhook-url: ${{ secrets.WEBHOOK_URL }}
