name: Test VapourSynth Setup Action

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Run local Discord Failure Notifier
        uses: ./
